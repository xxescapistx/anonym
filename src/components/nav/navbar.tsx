import React from 'react'
import NavigationButtons from './navigation-buttons'
import { Flex, Group, UnstyledButton } from '@mantine/core'

const NavBar = () => {
  return (
    <NavigationButtons where='nav' />
  )
}

export default NavBar